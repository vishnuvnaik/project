var assert = require('chai').assert;

let callFunction = require('../Datastructures/bankingCash/bankingCounter');
describe("", () => {
    it("banking", () => {
        assert.equal(callFunction.bankingCash(1), people(20000), withdraw(5000), userInput(cancel));
    });
    it("banking", () => {
        assert.equal(callFunction.bankingCash(4), people(20000), withdraw(5000), userInput(cancel));
    });
});