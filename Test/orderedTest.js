var assert = require('chai').assert;

let callFunction = require('../Datastructures/orderedList/orderedList');
describe("ordered list", () => {
    it("read files", () => {
        assert.equal(callFunction.ordered());
    });
    it("perform operations", () => {
        assert.equal(callFunction.ordered());
    });
});