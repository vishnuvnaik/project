var assert = require('chai').assert;

let callFunction = require('../Algorithm Programs/Anagram/Anagram');
describe("payment", () => {
    it("payment", () => {
        assert.equal(callFunction.anagramDetect(madam, adama));
    });
    it("payment1", () => {
        assert.equal(callFunction.anagramDetect(bada, abad));
    });
});